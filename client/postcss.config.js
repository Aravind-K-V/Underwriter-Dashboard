// postcss.config.js
// Configuration file for PostCSS, defining plugins for processing CSS in the Underwriter Dashboard project.

// Export the PostCSS configuration object
export default {
  // Specify PostCSS plugins to process CSS
  plugins: {
    // Enable Tailwind CSS plugin for utility-first styling
    tailwindcss: {},
    // Enable Autoprefixer plugin to add vendor prefixes for cross-browser compatibility
    autoprefixer: {},
  },
};